{{- if and .Values.exporter.enabled .Values.exporter.rbac.create .Values.exporter.imagePullSecrets }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "container-image-exporter.fullname" . }}-image-pull-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "container-image-exporter.exporter.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "container-image-exporter.fullname" . }}-image-pull-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "container-image-exporter.exporter.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "container-image-exporter.fullname" . }}-image-pull-secrets
subjects:
  - kind: ServiceAccount
    name: {{ include "container-image-exporter.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
