name: Test Helm

on:
  push:
    branches:
      - main
    paths:
      - 'deploy/chart/**'
      - 'scripts/test-helm-*.sh'
      - 'Makefile'
      - '.github/workflows/test-helm.yaml'
      - 'Dockerfile'
      - 'go.mod'
      - 'go.sum'
      - '**/*.go'
  pull_request:
    paths:
      - 'deploy/chart/**'
      - 'scripts/test-helm-*.sh'
      - 'Makefile'
      - '.github/workflows/test-helm.yaml'
      - 'Dockerfile'
      - 'go.mod'
      - 'go.sum'
      - '**/*.go'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Cancel superseded PR runs to save CI minutes; queue main pushes so each
  # commit gets a full test report.
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions: {}

jobs:
  test-helm:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Lint
        run: make test-helm-lint

      - name: Unit tests
        run: make test-helm-unit

      - name: Integration tests
        run: make test-helm-integration
